version: '3.8'

services:
  soarengine:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ABUSEIPDB_API_KEY=${ABUSEIPDB_API_KEY:-}
      - VIRUSTOTAL_API_KEY=${VIRUSTOTAL_API_KEY:-}
    volumes:
      - .:/app
    restart: unless-stopped

  # Future additions ready to go:
  # redis:
  #   image: redis:alpine
  #   ports:
  #     - "6379:6379"
  
  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: soarengine
  #     POSTGRES_USER: soar
  #     POSTGRES_PASSWORD: securepassword
  #   ports:
  #     - "5432:5432"